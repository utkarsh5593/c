#include <stdio.h>

int main()
{
    int r = 3;
    float pi = 3.14;
    printf("The area of this circle is %f", pi * r * r);
    int height = 3;
    printf("Volume of this cylinder is %f", pi * r * r * height);
    return 0;
}